meta {
  name: Update Stock
  type: http
  seq: 4
}

patch {
  url: {{base_url}}/inventory/products/1/stock
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "adjustment": 10,
    "reason": "Restock"
  }
}
