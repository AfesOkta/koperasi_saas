meta {
  name: Deposit
  type: http
  seq: 1
}

post {
  url: {{base_url}}/savings/transactions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "account_id": 1,
    "type": "deposit",
    "amount": 100000,
    "notes": "Initial deposit"
  }
}
