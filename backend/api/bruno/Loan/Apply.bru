meta {
  name: Apply for Loan
  type: http
  seq: 3
}

post {
  url: {{base_url}}/loans/apply
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "member_id": 1,
    "product_id": 1,
    "amount": 5000000,
    "term_months": 12,
    "purpose": "Modal Usaha"
  }
}
