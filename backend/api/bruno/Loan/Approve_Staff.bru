meta {
  name: Approve by Staff
  type: http
  seq: 6
}

post {
  url: {{base_url}}/loans/1/approve/staff
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "action": "approve",
    "notes": "Dokumen lengkap, disetujui oleh staff"
  }
}
