meta {
  name: Approve by Manager
  type: http
  seq: 8
}

post {
  url: {{base_url}}/loans/1/approve/manager
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "action": "approve",
    "notes": "Final approval oleh manager, pinjaman dicairkan"
  }
}
